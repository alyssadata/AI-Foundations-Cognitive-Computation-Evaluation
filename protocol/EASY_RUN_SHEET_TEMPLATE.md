# OPTIONAL TEMPLATE — `EASY_RUN_SHEET.md`

**Use when the external evaluation includes a repeatable run or interaction.**

This file is the operator-facing execution layer. It should be easier to follow than the formal protocol and must not silently add criteria that are absent from the frozen protocol.

**Evaluator:** AI Foundations  
**Evaluation author:** Alyssa Solen  
**Source-line:** Alyssa Solen → AI Foundations → Origin | Continuum  
**Repository:** [REPOSITORY NAME]  
**External source:** [EXTERNAL SOURCE TITLE]  
**External author / creator:** [EXTERNAL AUTHOR / CREATOR]  
**External source version / identifier:** [VERSION / IDENTIFIER / UNKNOWN]  
**Claim / test ID:** [ID]  
**Version:** [VERSION]

---

## External-Source Boundary

This run evaluates an independently authored external source.

**AI Foundations is the evaluator. The external source remains external to AI Foundations.**

Running, supporting, weakening, reproducing, or discussing the external claim does not incorporate it into AI Foundations or AI Foundations canon.

---

## What This File Is

This is the exact operator-facing run sheet for `[EVALUATION / TEST NAME]`.

The operator should not need to reinterpret the external theory or the formal protocol during execution.

Use direct instructions such as:

`open environment → verify source/test version → prepare condition → run exact input → preserve output → continue → save final record`

---

# BEFORE YOU START

## 1. Verify the evaluated source

Confirm that the source matches the repository source record.

```text
EXTERNAL SOURCE TITLE:
EXTERNAL AUTHOR / CREATOR:
SOURCE LOCATION:
VERSION / DATE / IDENTIFIER:
CLAIM / MATERIAL UNDER EVALUATION:
```

If the source version cannot be verified, stop or record `UNKNOWN` according to the formal protocol.

## 2. Open the instance / environment

[State exactly what to open.]

State whether prior history, memory, tools, files, software versions, or other context must be on, off, preserved, or recorded.

## 3. Prepare the input / stimulus

[State exactly what the operator should attach, paste, upload, enter, initialize, or execute.]

Preserve the exact source input used.

## 4. Start condition

[State what the operator does before the first formal step.]

---

# RUN

Execute each block exactly as written, one at a time, in order.

Do not paraphrase prompts or procedures.
Do not combine steps.
Do not silently repair outputs.
Do not add improvised follow-ups unless this run sheet explicitly permits them.
Do not reinterpret the external claim during the run.

---

## STEP 1 — [STEP NAME]

```text
[EXACT PROMPT / INPUT / PROCEDURE]
```

Preserve the complete output exactly.

---

## STEP 2 — [STEP NAME]

```text
[EXACT PROMPT / INPUT / PROCEDURE]
```

Preserve the complete output exactly.

---

## STEP 3 — [STEP NAME]

```text
[EXACT PROMPT / INPUT / PROCEDURE]
```

Preserve the complete output exactly.

---

## ADD OR REMOVE STEPS AS REQUIRED

The number of steps depends on the external claim and frozen protocol.

Do not force a fixed number of steps across external evaluations.

---

# FINAL COLLECTION OUTPUT

If useful, include one final archival step that requests or records:

- external source / claim ID;
- run metadata;
- final protocol-defined outcome;
- criterion results if applicable;
- deviations;
- raw output or complete visible transcript;
- and evidence pointers.

Use `UNKNOWN` for unavailable metadata.

For model-generated transcripts, require `TRANSCRIPT ACCESS INCOMPLETE` rather than reconstruction of missing turns.

## FINAL STEP — Create Complete Run Record

```text
Create the final archival record for this external evaluation run.

Do not invent unavailable metadata.
For any field you cannot directly know or verify, write UNKNOWN.
Do not summarize or repair raw outputs.
If transcript access is incomplete, write TRANSCRIPT ACCESS INCOMPLETE rather than reconstructing missing content.

Return these sections:

1. EXTERNAL SOURCE / CLAIM IDENTITY
[INSERT REQUIRED FIELDS]

2. RUN METADATA
[INSERT REQUIRED METADATA FIELDS]

3. FINAL STATUS / OUTCOME
[INSERT EXACT ALLOWED OUTCOMES AND DECISION LANGUAGE]

4. CRITERIA / MEASURES, IF APPLICABLE
[INSERT EVALUATION-SPECIFIC FIELDS]

5. DEVIATIONS / MISSING DATA
[INSERT REQUIRED FIELDS]

6. VERBATIM TRANSCRIPT OR RAW OUTPUT
[INSERT REQUIRED RAW EVIDENCE FORM]

7. ARCHIVAL INTEGRITY NOTE
State whether the primary run evidence is complete.
```

---

# AFTER THE RUN

Save the evaluation-specific primary evidence, which may include:

1. the original interface / runtime record;
2. exact prompts, inputs, or stimulus files;
3. raw model / system / software outputs;
4. source and claim identifiers;
5. operator-recorded metadata;
6. hashes, commits, logs, exports, or screenshots when relevant.

The original evidence remains primary. Do not silently replace it with a later summary.

---

# EASY FINAL RULE

[Write one plain-language sentence explaining what must happen before this external claim receives the protocol-defined result.]

Then preserve the boundary:

**The result evaluates the external claim. It does not make the external claim part of AI Foundations.**

---

**AI Foundations evaluation source-line:** Alyssa Solen → AI Foundations → Origin | Continuum
